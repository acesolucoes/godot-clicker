extends Sprite2D

func _ready() -> void:
	#mouse_filter = MouseFilterEnum.Stop
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta: float) -> void:
	pass
